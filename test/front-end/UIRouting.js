'use strict';

describe('UI ROUTER', function () {
  // var $rootScope, $stateParams, $scope, $state, $injector;
  // beforeEach(module('trivial'));

  // beforeEach(inject(function ($injector) {
  //   $rootScope = $injector.get('$rootScope');
  //   $stateParams= { gameId: 390923 };
  //   $scope = $rootScope.$new();
  // }));

  // it('Should have /login route, template, and controller', function () {
  //   expect($state.routes['/login']).to.be.defined;
  //   expect($state.routes['/login'].controller).to.equal('LoginCtrl');
  //   expect($state.routes['/login'].templateUrl).to.equal('templates/login.html');
  // });

  // it('Should have /signin route, template, and controller', function () {
  //   expect($state.routes['allgames']).to.be.defined;
  //   expect($state.routes['allgames'].controller).to.equal('AllGamesCtrl as allgames');
  //   expect($state.routes['allgames'].templateUrl).to.equal('templates/allgames.html');
  // });

  // it('Should have /links route, template, and controller', function () {
  //   expect($state.routes['/games/:gameId']).to.be.defined;
  //   expect($state.routes['/games/:gameId'].controller).to.equal('GamesCtrl');
  //   expect($state.routes['/games/:gameId'].templateUrl).to.equal('templates/games.html');
  // });

});
